import { InputPropsTw } from '@types';

export const inputDefaultProps: Partial<InputPropsTw> = {
  allow: 'all',
  editable: true,
  font: 'primaryRegular',
  hasClearIcon: true,
  placeholderTextColor: '#BDBDBD',
  size: 'md',
};
