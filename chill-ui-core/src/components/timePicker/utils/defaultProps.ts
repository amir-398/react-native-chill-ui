import { TimePickerScrollerPropsTw } from '@types';

export const timePickerDefaultProps = {
  data: [],
  hoursInterval: 1,
  minutesInterval: 5,
  mode: 'hour' as TimePickerScrollerPropsTw['mode'],
  secondsInterval: 10,
};
