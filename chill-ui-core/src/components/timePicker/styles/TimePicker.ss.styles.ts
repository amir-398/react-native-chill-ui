import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 8,
    overflow: 'hidden',
    position: 'relative',
    width: '100%',
  },
  contentContainer: {
    alignItems: 'center',
    gap: 4,
    justifyContent: 'center',
  },
  listItem: {
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 6,
  },
});
