import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    overflow: 'hidden',
  },
  disabled: {
    opacity: 0.5,
  },
  pressable: {
    position: 'relative',
  },
});

export default styles;
