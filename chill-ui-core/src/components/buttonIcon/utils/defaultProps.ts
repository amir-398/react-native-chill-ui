import { ButtonIconPropsTw } from '@types';

export const buttonIconDefaultProps: Partial<ButtonIconPropsTw> = {
  as: 'touchable-opacity',
  colorVariant: 'primary',
  isDisabled: false,
  isLoading: false,
  rounded: 'square',
  variant: 'contained',
};
