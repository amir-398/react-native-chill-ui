import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputDropdownBase: {
    backgroundColor: 'white',
    borderColor: '#E5E7EB',
    borderRadius: 8,
    borderWidth: 1,
  },
  inputDropdownBaseShadow: {
    shadowColor: '#000',
    shadowOffset: { height: 1, width: 0 },
    shadowOpacity: 0.2,
    shadowRadius: 1.41,
  },
  searchInputContainer: {
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
});
